# ── src/ new rewrite ──────────────────────────────────────────────────────────
src/backend/ginbar
src/backend/tmp/
src/backend/public/
src/backend/sessions.sqlite3
src/backend/vendor/
src/backend/data/
src/backend/.env
src/frontend/node_modules/
src/frontend/dist/
src/frontend/.env.local
.env

# ── Redis snapshot (created in cwd by the Redis server) ──────────────────────
dump.rdb

# ── pnpm ──────────────────────────────────────────────────────────────────────
.pnpm-store/

# ── TLS / Cloudflare Origin Certificate (never commit private keys) ───────────
nginx/certs/*.crt
nginx/certs/*.key
nginx/certs/*.pem

# ── VSCode settings (personal preferences) ─────────────────────────────────
.github/

# ── Rust / Cargo ──────────────────────────────────────────────────────────────
src/worker/target/
src/worker/Cargo.lock
src/worker/**/*.rs.bk
src/worker/.cargo/
